# Camera demo example using OpenCV

Steps to show the example:

```
$ mkdir build
$ conan install ..
$ cmake .. -G "Visual Studio 15 2017 Win64"
$ cmake --build . --config Release
$ cd bin
$ camera
```